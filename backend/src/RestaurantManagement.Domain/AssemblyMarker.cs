namespace RestaurantManagement.Domain;

/// <summary>
/// Anchor type used to reference the domain assembly. No entities exist yet.
/// </summary>
public static class AssemblyMarker
{
    /// <summary>The assembly that will contain domain entities and rules.</summary>
    public static readonly System.Reflection.Assembly Assembly = typeof(AssemblyMarker).Assembly;
}
